Implemented and pushed to `main`.

What changed:
- Added parser/recent game statuses:
  `riot_account_not_linked`, `riot_access_not_configured`, `checking_recent_games`, `recent_games_unavailable`, `games_found_parsing`, `some_games_ready`, `all_recent_games_ready`, `parse_failed_retry_available`.
- Recent game API now returns cached ready games immediately and starts uncached match fetch/parse in the background.
- UI shows `X games ready` and `Y games still being prepared`.
- Ready candidate games remain visible with a `Review` link, including when only one game is ready.
- Added an in-memory guard to avoid duplicate background preparation for the same match during refreshes.
- Added/updated server and jsdom tests.

Verification:
- `npm test` passed: 15 files, 74 tests.

Commit:
- `44b1608 Add non-blocking Riot parser status`

Untracked pre-existing local files were left untouched.
