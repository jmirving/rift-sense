Final validation complete. No commit made.

Changed files:
- `server/riot/recent-games.js`
- `server/config.js`
- `tests/server/riot-recent-games.test.mjs`

What changed:
- Aligned Riot recent match default lookup limit to documented `10`.
- Enforced max `5` new queued matches per repository-backed refresh.
- Marked successfully parsed stored perspectives as `parsed`.
- Added regression tests for lookup and queue limits.

Tests run:
- `npm test` passed: 19 files, 90 tests.
- `lint`, `build`, and `typecheck` package scripts are absent.

Known limitations:
- No separate lint/build/typecheck commands are available in `package.json`.

Human decision required:
- No.
