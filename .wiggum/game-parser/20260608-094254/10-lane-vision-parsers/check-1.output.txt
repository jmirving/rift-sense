PASS

Evidence reviewed:
- Task requirements in `scripts/wiggum-game-parser-loop.sh`.
- Lane parser: `server/riot/lane-pressure.js` implements pre-14-minute deltas, lane opponent resolution, plates, deaths, and required tags.
- Vision parser: `server/riot/vision-information.js` implements ward/objective/death/push evidence with cautious wording and confidence.
- Integration: `server/riot/recent-games.js` includes both parsers in match evidence aggregation.
- Tests: `tests/server/riot-lane-pressure.test.mjs`, `tests/server/riot-vision-information.test.mjs`.

Commands run:
- `npx vitest run tests/server/riot-lane-pressure.test.mjs tests/server/riot-vision-information.test.mjs` passed: 2 files, 5 tests.
- `npm test` passed: 19 files, 88 tests.
- `git diff -- ...` showed no uncommitted tracked diffs for reviewed files.

Failures found:
- None.

Note:
- Working tree has unrelated untracked local/support files, but no tracked implementation diffs needing action.
