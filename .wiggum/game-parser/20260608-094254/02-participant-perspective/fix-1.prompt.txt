You are fixing task '02-participant-perspective' in the RiftSense repository.

The checker found issues. Fix them without asking for confirmation unless a human decision is strictly required.

Checker output:

FAIL

Evidence reviewed:
- Task spec in `scripts/wiggum-game-parser-loop.sh`
- Resolver: [server/riot/participant-perspective.js](/home/jirving/projects/lol/rift-sense/server/riot/participant-perspective.js:24)
- Tests: [tests/server/riot-participant-perspective.test.mjs](/home/jirving/projects/lol/rift-sense/tests/server/riot-participant-perspective.test.mjs:51)
- Integration: [server/riot/recent-games.js](/home/jirving/projects/lol/rift-sense/server/riot/recent-games.js:166)

Commands run:
- `git status --short`
- `rg -n "02-participant-perspective|participant perspective|Participant Perspective|participant" -S .`
- `npm test -- --run tests/server/riot-participant-perspective.test.mjs tests/server/riot-recent-games.test.mjs tests/server/riot-matches.repository.test.mjs`

Validation result:
- Focused tests pass: 3 files, 14 tests.

Failure found:
- Timeline fallback is implemented against the wrong shape. `findTimelineParticipantId()` expects `metadata.participants` entries with `{ puuid, participantId }`, but Match-V5 metadata participants are PUUID strings. This means summary-without-`participantId` plus real timeline metadata will fail instead of resolving participant ID.

Exact fix:
- Update `findTimelineParticipantId()` to support string arrays by finding `puuid` index and returning `index + 1`.
- Keep object-array support if desired.
- Change the fallback test to use `metadata.participants: ["other", "puuid_1"]` and expect `participantId: 2`.

Requirements:
- Address every fixable issue from the checker.
- Keep scope limited to the current task.
- Preserve existing behavior outside the task.
- Add or update tests when appropriate.
- Run the relevant tests again.
- If a human decision is truly required, explain exactly what decision is required and why.
