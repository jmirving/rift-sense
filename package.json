{
  "name": "rift-sense",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "local:mvp": "node scripts/local-mvp.mjs",
    "local:mvp:auth": "node scripts/local-mvp.mjs --auth",
    "local:token": "node scripts/mint-dev-token.mjs",
    "start:api": "node server/index.js",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "multer": "^2.0.2",
    "pg": "^8.21.0"
  },
  "devDependencies": {
    "jsdom": "^28.1.0",
    "supertest": "^7.2.2",
    "vitest": "^4.0.18"
  }
}
